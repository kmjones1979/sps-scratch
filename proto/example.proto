syntax = "proto3";

package example;

message Contracts {
  repeated Contract contracts = 1;
}

message Contract {
    string address = 1;
    string timestamp = 2;
    uint64 blockNumber = 3;
    uint64 ordinal = 4;
}